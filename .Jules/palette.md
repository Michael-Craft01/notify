## 2024-04-09 - Missing ARIA labels on icon-only buttons
**Learning:** Icon-only buttons without `aria-label`s are a recurring accessibility issue across various UI components in this app.
**Action:** Always verify that every icon-only button includes a descriptive `aria-label` to ensure screen reader compatibility.
